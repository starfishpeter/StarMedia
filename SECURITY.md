# 安全策略

## 支持范围

StarMedia 目前处于早期开发阶段，安全修复只保证进入最新发布版本，不单独维护旧版本分支。

## 报告安全问题

请不要为尚未修复的漏洞创建公开 Issue。优先通过仓库的 **Security → Report a vulnerability** 私下提交报告，并包含：

- 受影响版本
- 可复现的最小步骤
- 潜在影响
- 建议修复方式（如有）

报告中不要附带真实令牌、私人媒体或完整的 `StarMediaData`。如果 GitHub 的私密报告入口尚未启用，请先通过维护者的 GitHub 主页联系维护者，再交换细节。

## 特别关注范围

欢迎报告路径穿越、任意文件覆盖、升级包校验绕过、IPC 权限绕过、令牌泄露，以及恶意媒体或压缩包导致的安全问题。
