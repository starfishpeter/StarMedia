# 更新日志

本项目从公开版本开始采用接近 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，版本号遵循语义化版本。

仅保留当前版本系列的更新记录，避免日志无限增长。当前保留 `0.7.x`；升级到下一个版本系列时，应移除本系列记录并从新系列开始。完整历史可通过 [GitHub Releases](https://github.com/starfishpeter/StarMedia/releases) 和 Git 提交记录查询。

## [0.7.8] - 2026-07-20

### 改进

- 单集标题明确记录为 Bangumi 或手动维护来源。Bangumi 标题重命名后仍会保留正篇 `#05`、特别篇 `#SP01` 等自动编号；手动编辑的名称保持原样。
- Bangumi 特别篇会使用 `#SP01`、`#SP02` 命名，排序位于正篇之后，不再误作 `#01`、`#02` 正篇。
- Bangumi 章节分配优先解析本地名称中的 `#04`、`EP04` 或“第 04 话”等显式选集标记，不会将作品名里的季数误认为集数；单话本地合集在 Bangumi 仅有一个正篇章节时可直接分配。
- 应用 Bangumi 资料后会将其日语原名回填到网络刮削搜索框；导入预览移除冗余的关联字幕统计卡，关联字幕仍随视频导入。
- 清理已淘汰的开发数据目录、旧刮削来源与旧书籍封面迁移分支，保留当前数据与文件操作的安全保护。
- 本地 `#OVA02`、`#SP02` 等特别篇标记仅匹配 Bangumi 特别篇；无法匹配时保持未关联，绝不错误关联正篇或将作品季数当作集数。

## [0.7.7] - 2026-07-19

### 新增

- 视频合集可单独标记“内嵌字幕标记”，状态会即时同步保存到该合集所有选集；开启“额外信息”后以橙色角标显示，并可与蓝色“外挂字幕”角标并列。
- 合集详情新增删除入口。二次确认后会将整个受管理合集目录移入 Windows 回收站，并移除目录内全部索引记录；操作前会校验根目录、目录一致性和关联字幕路径。

### 改进

- 合集网络刮削每次展开时固定从 Bangumi 开始；内嵌字幕开关移动到“网络刮削”后的操作区，无需再保存编辑资料。
- 顶部搜索框缩短，并在搜索范围后增加“刷新媒体库”。刷新时会核对同目录实际外挂字幕文件，更新新增、删除或替换字幕后的角标。
- Bangumi 单集会保存章节排序号；无章节前缀的标题会补为 `#01 标题`，用于显示和视频、外挂字幕成组重命名。手动编辑选集名称后会同步更新显示标题。
- 使用 Bangumi 标题重命名时，文件名中的 Windows 非法字符会替换为全角等价字符，原始显示标题不受影响。
- 合集选集移除隐藏的“集数排序”，默认按显示名称 A-Z 自然排序；手动修改名称或编号后会立即反映在顺序中。
- 应用设置新增默认播放模式，可选择下次应用内播放默认使用适应窗口或影院模式；关闭前确认改为主题色滑动开关。
- 影院模式的画面外黑色区域支持点击播放/暂停和长按临时 2 倍速，播放器文字不再可被选中。
- 单话合集的 Bangumi 标题不再自动添加 `#01` 前缀；应用设置新增默认阅读模式，可选择下次打开本子或漫画默认单页或滚动。
- 应用内 ASS 字幕新增完整的内置 CJK Bold 字体，字幕脚本指定粗体时使用真实粗体字形。
- Bangumi 标题不再为已含中文、全角或单数字章节号的选集重复添加 `#01`；手动编辑的选集名称不会再被自动编号覆盖。单话本地合集在 Bangumi 仅有一个正篇章节时可直接分配。
- Bangumi 章节分配优先解析本地名称中的 `#04`、`EP04` 或“第 04 话”等显式选集标记，不会将作品名中的季数误认为章节号。
- Bangumi 特别篇会使用 `#SP01`、`#SP02` 命名，排序位于正篇之后，不再误作 `#01`、`#02` 正篇。
- 应用 Bangumi 资料后会将其日语原名回填到网络刮削搜索框；导入预览移除冗余的关联字幕统计卡，关联字幕仍随视频导入。
- 清理已淘汰的开发数据目录、旧刮削来源与旧书籍封面迁移分支，保留当前数据与文件操作的安全保护。
- 播放器将“原始尺寸”更名为“影院模式”，提供不会遮挡顶部栏和底部控制栏的居中等比观看区域。
- 统一主界面、阅读器和播放器顶部栏高度，优化侧栏品牌区和媒体库导航间距，并移除冗余的“媒体库”分组文字。
- 本子和漫画的无书架媒体墙改为从顶部自然排列，单项卡片保持与番剧海报一致的竖向尺寸，选中边框不再拉伸整列。
- 缓存说明明确上限仅覆盖阅读页、字幕转换和内嵌字幕提取缓存；缩略图与封面不受该上限限制。

## [0.7.6] - 2026-07-19

### 改进

- Bangumi 单集资料优先作为选集显示标题；本地集号继续仅用于稳定排序与文件定位。
- 单集详情新增“使用 Bangumi 标题重命名”，会将视频与已关联外挂字幕成组重命名，并沿用冲突预检和失败回滚。
- Bangumi 合集 ID 区新增显式“分配章节”操作，仅在点击后关联可确认的正篇章节；合集资料写入不再隐式分配。
- 刮削面板恢复全宽布局，Bangumi 搜索和 ID 两行操作按钮统一高度。

## [0.7.5] - 2026-07-19

### 改进

- Bangumi 合集刮削会自动关联集号可确认的正篇单集 ID；SP、PV、预告和其他非正篇条目不会自动关联。
- 视频详情新增独立的 Bangumi 单集区块，可输入或粘贴单集 ID 后刮削当前选集的显示标题、首播日期和单集简介；本地文件名与合集简介不受影响。
- Bangumi 合集 ID 旁新增“访问原页”操作；刮削面板收紧至易读宽度，并将制作公司与第一话首播日期并列编辑。
- 缩小顶部栏下方的首段留白，减少媒体墙与标题栏之间的空白。

## [0.7.4] - 2026-07-19

### 改进

- 合集简介默认最多显示六行，可按需展开或收起，长简介不再将选集区域推离首屏。
- 标签输入框改为应用内联想建议：输入关键词后显示最多八个匹配标签，仍支持直接输入新标签并按 Enter 添加。
- 统一主界面、漫画阅读器和应用内播放器的顶部栏高度；外挂字幕角标进一步贴近封面右下角。

## [0.7.3] - 2026-07-19

### 改进

- 外挂字幕角标开关更名为“额外信息”，合集封面角标改为蓝色“外挂字幕”；右键菜单新增在 Windows 文件管理器中打开所在目录。
- 媒体库合集墙支持 Ctrl+A 选中当前库的全部合集；单个视频的右键菜单不再显示跨媒体库转移。

### 修复

- 加载媒体库时自动补齐旧索引中遗漏的同目录同名外置字幕记录，已有的 ASS/SSA 等字幕合集可正确显示角标。

## [0.7.2] - 2026-07-19

### 改进

- 外挂字幕改为带“开/关”状态文字的滑动开关，并强化合集封面的“外挂”高对比度角标。

### 修复

- 从合集或书架返回媒体库列表时恢复进入前的滚动位置，不再因详情内容较短而回到顶部。

## [0.7.1] - 2026-07-19

### 改进

- 媒体库顶部统计在合集视图中显示合集数量，并显示配置根目录的实际磁盘占用，包含字幕、封面及未索引文件。
- 视频详情操作调整为固定两行：应用内/外部播放，以及编辑选集、替换文件和移入回收站；移除单集移动合集入口。
- 导入计划单独展示媒体项目与已关联字幕数量，未匹配媒体的字幕会明确提示，不再计入“不支持”。
- 媒体库移除分类和标签下拉筛选，保留搜索与排序；标签管理改为单页，移除分类、媒体库、制作公司和创作者管理页。
- 视频媒体库增加全局持久化的“外挂字幕”显示开关；打开后，任一选集关联外置字幕的合集封面会显示“外挂”角标。
- 媒体库顶部的合集数量与磁盘占用统一文字颜色、字号和字重。

### 修复

- 媒体墙的框选表面延伸到卡片后的底部空白区域，可从该区域开始拖动框选。

[0.7.1]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.1
[0.7.2]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.2
[0.7.3]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.3
[0.7.4]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.4
[0.7.5]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.5
[0.7.6]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.6
[0.7.8]: https://github.com/starfishpeter/StarMedia/releases/tag/v0.7.8
